import axios from 'axios'
// import type { PostQuestionItem, RequestSongListParams, RequestSongURLParams } from '@/types'

// const request = axios.create({
//     baseURL: import.meta.env.VITE_API_SERVER
// })

// request.defaults.headers.post['Content-Type'] = 'application/json;charset=utf-8'

// request.defaults.timeout = 2500

// const _get = (url: string, params?: any) => {
//     return request.get(url, { params }).then((res: any) => res.data)
// }

// const _post = (url: string, data?: any) => {
//     return request.post(url, data).then((res: any) => res.data)
// }
